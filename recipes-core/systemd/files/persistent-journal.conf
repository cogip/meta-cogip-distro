[Journal]
# Keep logs on disk (/var/log/journal) instead of the default volatile
# /run, so a boot's logs survive a power cycle and can be read off the
# SD card on a host (journalctl -D <sdroot>/var/log/journal) -- essential
# for debugging a headless board with no serial/network access.
Storage=persistent
