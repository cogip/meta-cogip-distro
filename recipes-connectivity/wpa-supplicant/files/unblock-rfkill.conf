[Service]
# The Pi 4 Wi-Fi can come up soft-blocked (rfkill) at boot, which stops
# wpa_supplicant from ever associating. Clear all rfkill soft-blocks
# right before wpa_supplicant starts. (- prefix: ignore if rfkill is
# missing / nothing to unblock.)
ExecStartPre=-/usr/sbin/rfkill unblock all
